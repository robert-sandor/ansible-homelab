---
- name: Deploy {{ app_name }}

  hosts: "{{ app_name }}"

  vars:
    task: deploy

  tasks:
    - name: Import {{ app_name }}
      ansible.builtin.import_role:
        name: "{{ app_name }}"
        tasks_from: "{{ task }}"
