TZ={{ timezone }}
HOSTNAME={{ hostname }}

PUID={{ ansible_facts.user_uid }}
PGID={{ ansible_facts.user_gid }}

VERSION={{ grafana_version }}
PG_VERSION={{ grafana_pg_version }}

DB_NAME=grafana
DB_USER=grafana
DB_PASS={{ grafana_pg_pass }}
