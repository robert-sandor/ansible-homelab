TZ="{{ timezone }}"
HOSTNAME="{{ hostname }}"

PAPERLESS_VERSION="{{ paperless_version }}"
TIKA_VERSION="{{ paperless_tika_version }}"
GOTENBERG_VERSION="{{ paperless_gotenberg_version }}"

VALKEY_VERSION="{{ paperless_valkey_version }}"

PG_VERSION="{{ paperless_pg_version }}"
PG_DATABASE="{{ paperless_db_name }}"
PG_USER="{{ paperless_db_user }}"
