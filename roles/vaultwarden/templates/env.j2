HOSTNAME="{{ hostname }}"
PUID="{{ ansible_facts.user_uid }}"
PGID="{{ ansible_facts.user_gid }}"

VERSION="{{ vaultwarden_version }}"
SIGNUPS_ALLOWED="{{ vaultwarden_allow_signups | lower }}"
SHOW_PASSWORD_HINT="{{ vaultwarden_show_password_hint | lower }}"

PG_VERSION="{{ vaultwarden_pg_version }}"
PG_DATABASE="{{ vaultwarden_db_name }}"
PG_USER="{{ vaultwarden_db_user }}"
