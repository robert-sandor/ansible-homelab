- name: Deploy CICD services
  vars:
    hostname: "{{ ansible_hostname }}.{{ domain }}"
    cicd_deploy_path: "{{ deploy_path }}/cicd"
  block:
    - name: Deploy containers
      ansible.builtin.import_tasks: ./containers.yml
