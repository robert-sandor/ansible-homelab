TZ={{ timezone }}
HOSTNAME={{ hostname }}

PUID={{ ansible_facts.user_uid }}
PGID={{ ansible_facts.user_gid }}

IMMICH_VERSION={{ immich_version }}
IMMICH_ML_TAG={{ '' if immich_hwaccel_ml == 'cpu' else '-' + immich_hwaccel_ml }}

IMMICH_DATA_PATH={{ immich_data_path }}

IMMICH_HW_TRANSCODING={{ immich_hwaccel_transcoding }}
IMMICH_HW_ML={{ immich_hwaccel_ml }}

DB_USER=immich
DB_NAME=immich
DB_PASS={{ immich_db_password }}
