HOSTNAME={{ ansible_hostname }}.{{ domain }}
CORE_HOSTNAME={{ core_host }}.{{ domain }}

TRAEFIK_VERSION={{ base_config.traefik.version }}
DIUN_VERSION={{ base_config.diun.version }}
DOCKER_SOCKET_PROXY_VERSION={{ base_config.docker_socket_proxy.version }}
PORTAINER_AGENT_VERSION={{ base_config.portainer_agent.version }}
