global:
  checkNewVersion: false
  sendAnonymousUsage: false

api:
  insecure: false

accessLog:
  bufferingSize: 100
  filters:
    statusCodes:
      - "400-599"

log:
  level: info

providers:
  docker:
    network: traefik
    exposedByDefault: false

entryPoints:
  http:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: https
  https:
    address: ":443"
    http:
      tls:
        certResolver: letsencrypt
        domains:
          - main: "{{ ansible_hostname }}.{{ domain }}"
            sans:
              - "*.{{ ansible_hostname }}.{{ domain }}"
  smtp:
    address: ":465"

certificatesResolvers:
  letsencrypt:
    acme:
      email: "{{ traefik_acme_email }}"
      storage: /letsencrypt/acme.json
      caServer: https://acme-{% if traefik_acme_staging %}staging-{% endif %}v02.api.letsencrypt.org/directory
      dnsChallenge:
        provider: "{{ traefik_dns_provider }}"
        resolvers:
          - 1.1.1.1
          - 1.0.0.1

metrics:
  prometheus:
    manualRouting: true
