HOSTNAME="{{ hostname }}"

VERSION="{{ manyfold_version }}"
PG_VERSION="{{ manyfold_pg_version }}"
VK_VERSION="{{ manyfold_vk_version }}"


PG_DATABASE=manyfold
PG_USER=manyfold
PG_PASS="{{ manyfold_db_pass }}"

